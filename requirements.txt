# Core Dependencies
rich>=13.0.0
pyyaml>=6.0
openai>=1.0.0
google-generativeai>=0.3.0
prompt-toolkit>=3.0.0
questionary>=2.0.0
requests>=2.31.0

# Vector Memory & Embeddings (for memory system)
chromadb>=0.4.0
sentence-transformers>=2.2.0

# Web Research & Scraping (for researcher agent)
beautifulsoup4>=4.12.0
requests-html>=0.10.0

# Code Analysis (for codebase graph)
# ast is stdlib - used for AST parsing, code relationship tracking, and semantic queries

# Optional: Web Framework (for examples)
flask>=3.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
