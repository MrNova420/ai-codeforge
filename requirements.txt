# Core Dependencies
rich>=13.0.0
pyyaml>=6.0
openai>=1.0.0
google-generativeai>=0.3.0
prompt-toolkit>=3.0.0
questionary>=2.0.0
requests>=2.31.0

# Performance & System Monitoring
psutil>=5.9.0
numpy>=1.24.0

# Vector Memory & Embeddings (for memory system)
chromadb>=0.4.0
sentence-transformers>=2.2.0

# Web Research & Scraping (for researcher agent)
beautifulsoup4>=4.12.0
requests-html>=0.10.0

# Web Framework & API (for WebSocket server and War Room UI)
fastapi>=0.104.0
uvicorn>=0.24.0
websockets>=12.0

# Code Analysis (for codebase graph)
# ast is stdlib - used for AST parsing, code relationship tracking, and semantic queries

# Optional: Docker SDK (for container execution)
docker>=7.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
